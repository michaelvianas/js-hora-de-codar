// Aprendendo if else encadeado

let nome = `Michael`;
let idade = 25;

if(nome != undefined && nome == `Guilherme`) {
  alert(`Nome está definido`);
}else if(nome == `Michael` && nome.length > 0 && idade == 50){
  alert(`O nome é ${nome}`);
}else{
  alert(`O nome é ${nome}`);
}